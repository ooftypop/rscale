<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Rscale</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      
      <% if user_signed_in? %>
        <li class="nav-item">
          <button class="nav-button"><%= link_to('Start Poll', new_poll_path, class: "link-button") %></button>
        </li>
        <li class="nav-item">
          <button class="nav-button"><%= link_to('Create Group', new_group_path, class: "link-button") %></button>
        </li>
        <li class="nav-item">
          <button class="nav-button"><%= link_to('Edit User', edit_user_registration_path(current_user.id), class: "link-button") %></button>
        </li>
        <li class="nav-item">
          <button class="nav-button"><%= link_to('Logout', destroy_user_session_path, method: :delete, class: "link-button") %></button>
        </li>

        <li class="nav-item">
        <% if policy(:admin).index? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Admin Dashboards
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <button class="nav-button"><%= link_to 'Users Index', admin_users_path %></button>
              <button class="nav-button"><%= link_to 'Polls index', admin_polls_path %></button>
            </div>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
</nav>
