$(".modal-title").html("Participants in: <%= @poll.title %>");
$(".modal-body").html("<%= escape_javascript(render partial: '/polls/participants_form', locals: { poll: @poll }) %>");
$("#myModal").modal("show");
