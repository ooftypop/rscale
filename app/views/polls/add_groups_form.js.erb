$(".modal-title").html("Add Groups to <%= @poll.title %>");
$(".modal-body").html("<%= escape_javascript(render partial: '/polls/add_groups_to_polls', locals: { poll: @poll }) %>");
$("#myModal").modal("show");
