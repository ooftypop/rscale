<div class="group-show" style="text-align: center;">
  <h5><%= @group.title %></h5>
  <h6><%= @group.description %></h6>
  <button><%= link_to 'Edit group', edit_group_path(@group) %></button>
  <button><%= link_to 'Add Emails', edit_user_emails_path(id: @group), remote: true %></button>
  <button><%= link_to 'Back', :back, class: "form-path-link " %></button>
  <% if @group.users.any? %>
    <input type="text" id="myInput" onkeyup="searchTable()" placeholder="Search for emails..." title="Type in a name">

    <div class="table-wrapper">
      <table id="myTable" class="fl-table">
        <thead>
          <tr>
            <th>Email</th>
            <th>Remove</th>
          </tr>
        </thead>
        <tbody>
          <% @group.users.each do |user| %>
            <tr>
              <td><%= user.email %></td>
              <td><%= link_to 'Remove', remove_user_path(user_id: user.id, group_id: @group.id) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% end %>
</div>

<script>
 searchTable();
</script>
